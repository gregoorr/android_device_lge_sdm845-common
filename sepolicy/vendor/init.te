allow init sysfs_lge_fingerprint:file { open read write };

allow init socket_device:sock_file { unlink setattr create };

allow init hal_fingerprint_default_exec:file { execute };

allow init vfat:dir search;

dontaudit init { bt_firmware_file firmware_file }:filesystem getattr;

allow init sysfs_livedisplay_tuneable:file { open write };
